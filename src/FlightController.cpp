#include <Arduino.h>
#include "Classtest.h"
#include "ExtraClass.h"

#define LED_BUILDIN PC13
// Class      Object
LED_Controls LED;
ExtraExtra test;

void setup() {
  // LED.Begin(LED_BUILTIN);
}

void loop() {
  
  // LED.ON(LED_BUILTIN);
  // delay(1000);
  // LED.OFF(LED_BUILTIN);
  // delay(1000);
  // LED.flash(LED_BUILTIN,100,10);
}
